/**
 *      UNDEFINED
 *          - palavra reservada que signica indefinido
 *          - algo que não vai existir
 *      NULL
 *          - nulo
 *          - objeto que não possui nada dentro
 *          - diferente de indefinido
 * 
 *      UNDEFINED VS NULL
 *          * undefined é algo que não vai exitir e null existe mas não tem nada nele
 */
console.log(null, undefined)